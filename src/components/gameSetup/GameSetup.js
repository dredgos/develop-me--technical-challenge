import AddPlayer from '../addPlayer/'
import PlayerList from '../playerList';

const GameSetup = () => {
    return (
        <>
            <AddPlayer />
            <PlayerList />
        </>
    );
};

export default GameSetup;