const SetupMessage = () => {
    return (
        <section>
            <p>The games can only begin when the numbers of players entered is a minimum of 4 and a power of 2 (4, 8, 16, 32 etc...)</p>
        </section>
    );
};

export default SetupMessage;