const Match = ({ player1, player2 }) => {
    return (

        
        <div>
            <button>{player1}</button>
            <button>{player2}</button>
        </div>
    );
};

export default Match;